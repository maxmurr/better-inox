Automated tests give us fast feedback. A well-written test provides evidence
that a new feature works as intended and alerts us when we make an
implementation mistake.

Write tests close to the code change to get this feedback sooner. When you
change the software's behavior, update or add a test at the same time.

<img
  src={'/images/four-pillars/slow-vs-fast-feedback.png'}
  alt={
    'Two feedback cycles separated by a blue arrow. Left: a Change step and an Assess step far apart with a clock labelled Time between them, and a red Feedback arrow returning from Assess to Change. Right: the same cycle compacted, with Assess immediately after Change and no clock.'
  }
  width={2137}
  height={392}
/>

Test-first development builds this fast feedback into your workflow. You
describe the behavior before implementing it, then quickly learn whether it
works.

But automated tests vary in execution speed. Consider these examples.

### Test case 1

```ts
import { buildPermissionPayload } from './build-permission-payload';

describe('buildPermissionPayload', () => {
  it('should build a valid payload from form data', () => {
    const formData = {
      userId: 'abc-123',
      resource: 'projects',
      attributes: ['read', 'write'],
    };

    const payload = buildPermissionPayload(formData);

    expect(payload).toEqual({
      user_id: 'abc-123',
      permissions: [
        { resource: 'projects', attribute: 'read' },
        { resource: 'projects', attribute: 'write' },
      ],
    });
  });
});
```

### Test case 2

```tsx
describe('PermissionsForm', () => {
  beforeEach(() => {
    cy.intercept('POST', '/api/permissions', {
      statusCode: 200,
      body: { success: true },
    }).as('savePermissions');

    cy.mount(<PermissionsForm />);
  });

  it('should submit valid permissions data', () => {
    cy.get('input[name="userId"]').type('abc-123');
    cy.get('select[name="resource"]').select('projects');
    cy.get('input[name="attributes"]').type('read,write');
    cy.get('button').contains('Save').click();

    cy.wait('@savePermissions')
      .its('request.body')
      .should('deep.equal', {
        user_id: 'abc-123',
        permissions: [
          { resource: 'projects', attribute: 'read' },
          { resource: 'projects', attribute: 'write' },
        ],
      });

    cy.get('[data-testid="success-message"]').should('be.visible');
  });
});
```

### Test case 3

```ts
describe('Attribute-Based Permissions Page - E2E', () => {
  it('should register permissions end-to-end', () => {
    cy.visit('http://localhost:3000/permissions');

    cy.get('input[name="userId"]').type('abc-123');
    cy.get('select[name="resource"]').select('projects');
    cy.get('input[name="attributes"]').type('read,write');
    cy.get('button').contains('Save').click();

    // Send a real API request to the backend.
    cy.get('[data-testid="success-message"]').should(
      'contain.text',
      'Permissions saved successfully'
    );
  });
});
```

<PopQuestion
  id={'pq-tests-as-feedback-longest'}
  prompt={'Which test probably takes the longest to run?'}
  correctOptionId={'c'}
  options={[
    {
      id: 'a',
      text: 'Test case #1',
    },
    {
      id: 'b',
      text: 'Test case #2',
    },
    {
      id: 'c',
      text: 'Test case #3',
    },
  ]}
  explanation={
    '**Test case 3** is the slowest because it runs the full user interface and sends requests over the network. Test case 2 also runs and interacts with a user interface, but it stubs API requests, so it comes second. Test case 1 is fastest because it is a unit test with no out-of-process dependencies.'
  }
/>
